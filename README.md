# 📦 Borrow-Return Item System (Backend)

ระบบจัดการการยืม-คืนอุปกรณ์ พัฒนาด้วย **Node.js**, **Express** และ **MySQL** สำหรับบันทึกข้อมูลการใช้งานอุปกรณ์ในองค์กร

## 📂 โครงสร้างโปรเจกต์
- `backend/`: โฟลเดอร์หลักที่เก็บไฟล์โค้ด Node.js
- `backend/borrow-system-backend.js`: ไฟล์หลักในการรัน Server และ API
- `dump-my_database.sql`: ไฟล์สำรองฐานข้อมูล (สำหรับ Restore ข้อมูล)

---

## 🚀 ขั้นตอนการติดตั้งและรันโปรเจกต์

### 1. เตรียมฐานข้อมูล (Database)
- เปิดโปรแกรมจัดการฐานข้อมูล (เช่น DBeaver หรือ MySQL Workbench)
- สร้าง Database ชื่อ `my_database`
- นำเข้าข้อมูล (Import) โดยใช้ไฟล์: `dump-my_database-202601071327.sql`

### 2. ติดตั้ง Dependencies
เปิด Terminal แล้วเข้าไปที่โฟลเดอร์ backend:
```bash
cd backend
npm install

# 📦 Borrow-Return Item Management System (Backend)

ระบบหลังบ้านสำหรับการจัดการยืม-คืนอุปกรณ์ไอที พัฒนาด้วย Node.js, Express และ MySQL รองรับการจัดการรูปภาพอุปกรณ์

## 🚀 ฟีเจอร์หลัก (Key Features)
- **Item Management**: เพิ่ม, ลบ, ค้นหา และดูรายการอุปกรณ์ทั้งหมด
- **Image Upload**: รองรับการอัปโหลดรูปภาพอุปกรณ์จริงผ่าน API (ใช้ Multer)
- **Borrow & Return System**: ระบบยืม-คืนอุปกรณ์พร้อมบันทึกประวัติและหมายเหตุ
- **Data Validation**: 
  - ห้ามลบอุปกรณ์ที่กำลังถูกยืมอยู่
  - ห้ามคืนอุปกรณ์ที่ถูกคืนไปแล้ว
  - ตรวจสอบความถูกต้องของข้อมูลก่อนลง Database

## 🛠 การติดตั้ง (Installation)

1. ติดตั้ง dependencies:
   ```bash
   npm install